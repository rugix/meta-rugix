# yaml-language-server: $schema=https://raw.githubusercontent.com/siemens/kas/refs/heads/master/kas/schema-kas.json
header:
  version: 14
  includes:
    - examples/include/common.yaml

local_conf_header:
  image-conf: |
    IMAGE_FSTYPES = "tar.bz2 wic wic.bmap rugixb rugixb.hash"
    # Override the A/B system partition size (default: 4GiB).
    # RUGIX_SYSTEM_SIZE = "4GiB"
    # Build U-Boot firmware for QEMU pflash.
    EXTRA_IMAGEDEPENDS += "u-boot"

machine: qemuarm64
distro: poky
target:
  - core-image-minimal

defaults:
  repos:
    branch: scarthgap

repos:
  poky:
    url: https://github.com/yoctoproject/poky.git
    layers:
      meta:
      meta-poky:
      meta-yocto-bsp:

  meta-openembedded:
    url: "https://github.com/openembedded/meta-openembedded.git"
    layers:
      meta-oe:
      meta-python:
      meta-networking:
      meta-multimedia:
      meta-filesystems:

  meta-lts-mixins-rust:
    url: "https://git.yoctoproject.org/meta-lts-mixins"
    branch: scarthgap/rust

  meta-lts-mixins-uboot:
    url: "https://git.yoctoproject.org/meta-lts-mixins"
    branch: scarthgap/u-boot

  meta-rugix:
    layers:
      meta-rugix-core:
      meta-rugix-qemu-arm64-uboot:
